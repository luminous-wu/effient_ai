# 脱敏关键词黑名单（CLAUDE.md §8 IP 边界的可执行化）
#
# 每行一条 Python 正则（大小写不敏感）。命中即阻断提交。
# 真实专有标识只有你知道 —— 默认空，由你维护，故意不预填以免误伤本库正文
# （本库正文合法地讨论 .map/.elf/ARXML 等主题词，绝不能进黑名单）。
#
# 维护建议：放"绝不该出现在公开笔记里"的内部专有 token，例如
#   - 公司/项目内部代号（改名表里的原名）
#   - 内部 ECU / 模块前缀
#   - 专有寄存器/常量命名规律
#
# 示例（保持注释，按需取消注释并替换为你的真实 token）：
# ^.*INTERNAL_PROJECT_CODENAME.*$
# \bAcmeCorp_[A-Za-z0-9_]+\b
# \b0xDEADBEEF\b
